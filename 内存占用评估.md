# OpenClaw 内存占用评估报告

## 一、项目概况

**项目名称**：OpenClaw
**版本**：2026.2.2
**类型**：多渠道消息网关 + AI 代理平台
**目标**：在 1GB 内存机器上运行

---

## 二、主要内存占用分析

### 2.1 极高内存占用模块（单个可达 500MB+）

#### 1. 本地 LLM 模型运行（★★★★★ 最高优先级优化）
- **依赖**：`node-llama-cpp` (peerDependency)
- **位置**：`src/memory/embeddings.ts`, `src/memory/node-llama.ts`
- **内存占用**：300MB - 2GB+（取决于模型大小）
- **影响**：
  - 默认嵌入模型：`embeddinggemma-300M-Q8_0.gguf` (~300MB)
  - 如果用户加载大型对话模型，可能占用 1-8GB
- **优化建议**：
  - ✅ **完全禁用本地模型**，强制使用云端 API（OpenAI/Gemini）
  - 配置：`agents.defaults.memorySearch.provider = "openai"` 或 `"gemini"`
  - 不安装 `node-llama-cpp` 依赖

#### 2. 浏览器自动化（★★★★★ 最高优先级优化）
- **依赖**：`playwright-core` (包含 Chromium 内核)
- **位置**：`src/browser/pw-session.ts`, `src/browser/pw-tools-core.*.ts`
- **内存占用**：200MB - 800MB（每个浏览器实例）
- **影响**：
  - 无头 Chromium 进程基础占用：~150-300MB
  - 多标签页/复杂页面：可达 500-800MB
  - 并发多个页面时呈倍数增长
- **优化建议**：
  - ✅ **完全禁用浏览器功能**
  - 移除 `playwright-core` 依赖
  - 或通过环境变量 `OPENCLAW_DISABLE_BROWSER=1` 禁用

### 2.2 高内存占用模块（100-300MB）

#### 3. WhatsApp 通道（Baileys）
- **依赖**：`@whiskeysockets/baileys`
- **位置**：`src/web/` (13个文件)
- **内存占用**：80-250MB
- **影响**：
  - 维护 WebSocket 长连接
  - 会话状态同步（历史消息、联系人、群组）
  - 媒体缓存
- **优化建议**：
  - ⚠️ 如果不需要 WhatsApp，完全禁用
  - 限制消息历史缓存数量
  - 定期清理媒体缓存

#### 4. 图像处理库（Sharp）
- **依赖**：`sharp` (原生二进制模块)
- **位置**：`src/media/image-ops.ts` (8处使用)
- **内存占用**：50-200MB（处理时峰值）
- **影响**：
  - 处理高分辨率图片时占用显著
  - 批量处理或并发操作时倍增
- **优化建议**：
  - 在 macOS 上使用系统自带的 `sips` 命令（环境变量 `OPENCLAW_IMAGE_BACKEND=sips`）
  - 限制并发图像处理数量
  - 减小最大图像尺寸限制

#### 5. PDF 处理
- **依赖**：`pdfjs-dist`
- **位置**：`src/media/input-files.ts`
- **内存占用**：30-150MB（处理大文件时）
- **优化建议**：
  - 禁用 PDF 处理功能
  - 限制 PDF 文件大小上限

### 2.3 中等内存占用模块（20-100MB）

#### 6. 多消息通道集成
- **依赖**：
  - `grammy` + `@grammyjs/*` (Telegram)
  - `@buape/carbon` (Discord)
  - `@slack/bolt` + `@slack/web-api` (Slack)
  - `signal-utils` (Signal)
  - `@line/bot-sdk` (Line)
  - `@larksuiteoapi/node-sdk` (Feishu/Lark)
- **位置**：
  - `src/telegram/`, `src/discord/`, `src/slack/`
  - `src/signal/`, `src/line/`, `src/feishu/`
- **内存占用**：每个通道 10-40MB
- **优化建议**：
  - ✅ **只启用必需的通道**
  - 通过配置文件禁用不使用的通道
  - 环境变量：`OPENCLAW_SKIP_CHANNELS=1` 跳过所有通道加载

#### 7. AI 代理框架
- **依赖**：
  - `@mariozechner/pi-agent-core`
  - `@mariozechner/pi-ai`
  - `@mariozechner/pi-coding-agent`
  - `@mariozechner/pi-tui`
- **内存占用**：40-80MB
- **优化建议**：
  - 核心功能，不建议移除
  - 禁用 TUI 界面（`pi-tui`）可节省 ~10MB

#### 8. 其他媒体/理解模块
- **位置**：
  - `src/media-understanding/`
  - `src/link-understanding/`
  - `src/media/` (媒体管道)
- **内存占用**：20-60MB
- **优化建议**：
  - 禁用链接智能解析
  - 简化媒体理解功能

### 2.4 基础运行时（不可裁剪）

- **Node.js 运行时**：~50-100MB
- **基础依赖**：~30-50MB
  - `express`, `hono` (Web 服务器)
  - `ws` (WebSocket)
  - `sqlite-vec` (向量数据库)
  - 配置管理、日志等

---

## 三、内存占用估算

### 3.1 当前完整配置（所有功能启用）
```
Node.js 基础运行时:        ~80MB
本地 LLM (embeddings):    ~350MB
Playwright (浏览器):      ~250MB
WhatsApp (Baileys):       ~120MB
图像处理 (Sharp):         ~60MB
PDF 处理:                 ~40MB
多消息通道 (6个):         ~150MB
AI 代理框架:              ~60MB
其他模块 + 缓存:          ~100MB
----------------------------------------
总计:                    ~1210MB (超出 1GB 限制)
```

### 3.2 最小化配置（1GB 内存目标）

**裁剪方案**：
```
Node.js 基础运行时:        ~80MB
AI 代理框架 (核心):       ~60MB
单一消息通道 (Telegram):   ~25MB
图像处理 (sips 后端):     ~20MB
基础媒体处理:             ~30MB
配置 + 会话管理:          ~40MB
云端 API (无本地模型):     ~10MB
缓冲区:                   ~100MB
----------------------------------------
总计:                    ~365MB (可在 1GB 内稳定运行)
```

---

## 四、具体优化方案

### 4.1 强制性裁剪（必须执行）

#### 1. 禁用本地 LLM 模型
**节省内存**：300-500MB

**方法 1 - 配置文件修改** (`~/.openclaw/config.yaml`):
```yaml
agents:
  defaults:
    memorySearch:
      provider: "openai"  # 或 "gemini"
      fallback: "none"
```

**方法 2 - 不安装 node-llama-cpp**:
```bash
# 重新安装时跳过 peerDependencies
npm install -g openclaw --omit=peer
```

#### 2. 禁用浏览器自动化
**节省内存**：200-300MB

**方法 1 - 环境变量**:
```bash
export OPENCLAW_DISABLE_BROWSER=1
```

**方法 2 - 移除依赖** (修改 `package.json`):
```json
{
  "dependencies": {
    // 移除或注释掉
    // "playwright-core": "1.58.1"
  }
}
```

#### 3. 只启用单一消息通道
**节省内存**：100-150MB

**配置示例** (`~/.openclaw/config.yaml`):
```yaml
channels:
  telegram:
    enabled: true
    token: "YOUR_TOKEN"

  # 禁用其他通道
  discord:
    enabled: false
  slack:
    enabled: false
  whatsapp:
    enabled: false
  signal:
    enabled: false
  line:
    enabled: false
  feishu:
    enabled: false
```

### 4.2 可选优化（推荐执行）

#### 4. 使用系统图像处理工具
**节省内存**：30-50MB

**macOS 环境**:
```bash
export OPENCLAW_IMAGE_BACKEND=sips
```

**Linux 环境**:
- 保留 Sharp，但限制并发：
```yaml
media:
  maxConcurrentProcessing: 1
  maxImageSize: 1024  # 降低最大尺寸
```

#### 5. 禁用 PDF 处理
**节省内存**：20-40MB

**代码层面修改**：需要在 `src/media/input-files.ts` 中条件跳过 PDF 导入

或通过配置限制文件类型：
```yaml
media:
  allowedTypes: ["image/jpeg", "image/png", "image/webp"]
  # 排除 PDF
```

#### 6. 限制内存缓存
```yaml
sessions:
  maxHistoryMessages: 50  # 降低历史消息数量

memory:
  maxVectorCacheSize: 1000  # 限制向量缓存

gateway:
  maxConcurrentRequests: 2  # 降低并发处理
```

### 4.3 环境变量汇总

创建 `~/.openclaw/.env` 或在启动脚本中设置：

```bash
# 核心优化
OPENCLAW_DISABLE_BROWSER=1
OPENCLAW_IMAGE_BACKEND=sips  # 仅 macOS
OPENCLAW_SKIP_CHANNELS=discord,slack,signal,line,feishu,whatsapp

# 内存限制
NODE_OPTIONS="--max-old-space-size=768"  # 限制 Node.js 堆为 768MB

# 调试（可选）
OPENCLAW_LOG_LEVEL=warn  # 减少日志输出
```

---

## 五、实施步骤

### 步骤 1：备份当前配置
```bash
cp ~/.openclaw/config.yaml ~/.openclaw/config.yaml.backup
```

### 步骤 2：重新安装（精简模式）
```bash
# 卸载现有版本
npm uninstall -g openclaw

# 安装时跳过 peerDependencies
npm install -g openclaw --omit=peer

# 或从源码构建时移除 playwright-core
```

### 步骤 3：修改配置文件
按照 **4.1** 和 **4.2** 章节修改 `~/.openclaw/config.yaml`

### 步骤 4：设置环境变量
在 `~/.bashrc` 或 `~/.zshrc` 中添加：
```bash
export OPENCLAW_DISABLE_BROWSER=1
export OPENCLAW_IMAGE_BACKEND=sips  # macOS only
export NODE_OPTIONS="--max-old-space-size=768"
```

### 步骤 5：重启服务
```bash
openclaw gateway run --bind loopback --port 18789
```

### 步骤 6：监控内存使用
```bash
# Linux
ps aux | grep openclaw
top -p $(pgrep -f openclaw)

# macOS
ps aux | grep openclaw
top -pid $(pgrep -f openclaw)
```

---

## 六、风险评估与建议

### 6.1 功能损失
| 裁剪模块 | 损失功能 | 影响程度 |
|---------|---------|---------|
| 本地 LLM | 离线嵌入搜索 | **低** - 云端 API 可替代 |
| 浏览器自动化 | 网页截图、自动化操作 | **中** - 如需浏览器功能则无法裁剪 |
| 多消息通道 | 多平台同时接入 | **中** - 根据实际需求选择 |
| PDF 处理 | PDF 文档理解 | **低** - 大多数场景用不到 |

### 6.2 稳定性建议
- **内存监控**：建议设置 `NODE_OPTIONS="--max-old-space-size=768"` 防止内存超限
- **OOM 保护**：在 1GB 机器上建议配置 swap（交换空间）至少 1GB
- **进程守护**：使用 `systemd` 或 `pm2` 确保异常退出后自动重启

### 6.3 最低配置建议

**如果必须在 1GB 内存机器运行**：
- ✅ 禁用本地 LLM（必须）
- ✅ 禁用浏览器自动化（必须）
- ✅ 只启用 1 个消息通道（必须）
- ✅ 使用系统图像工具（推荐）
- ✅ 限制并发和缓存（推荐）

**理想配置**：
- 2GB 内存：可启用 2-3 个消息通道 + 基础功能
- 4GB 内存：可启用所有消息通道（无本地 LLM）
- 8GB+ 内存：可启用完整功能（包括本地 LLM 和浏览器）

---

## 七、总结

### 7.1 可行性结论
✅ **在 1GB 内存机器上运行 OpenClaw 是可行的**，但需要：
- 禁用本地 LLM 模型
- 禁用浏览器自动化功能
- 只保留 1-2 个必需的消息通道
- 限制并发处理和缓存

### 7.2 预期内存占用
- **最小化配置**：350-400MB
- **推荐配置**：500-600MB（留有余量）
- **安全边界**：保持在 700MB 以下

### 7.3 核心优化收益
| 优化项 | 节省内存 | 实施难度 |
|--------|---------|---------|
| 禁用本地 LLM | ~350MB | ⭐ 简单 |
| 禁用浏览器自动化 | ~250MB | ⭐ 简单 |
| 减少消息通道 | ~120MB | ⭐⭐ 中等 |
| 优化图像处理 | ~40MB | ⭐⭐ 中等 |
| **总计** | **~760MB** | - |

### 7.4 后续建议
1. **分阶段实施**：先执行强制性裁剪，测试稳定后再考虑可选优化
2. **性能监控**：定期检查内存使用，及时调整配置
3. **功能平衡**：根据实际业务需求在功能和资源之间找到平衡点
4. **升级硬件**：如果预算允许，2GB 内存可获得更好的体验

---

**报告生成时间**：2026-02-04
**评估基准版本**：OpenClaw 2026.2.2
**适用场景**：低内存环境部署、边缘设备、受限资源服务器
